You are a senior FastAPI developer. Your task is to generate production-quality
Python backend code for a hackathon project.

INPUT:
API contracts (implement ONLY these endpoints):
{api_contracts}

Database schema:
{database_schema}

Component map (generate ONLY these files):
{component_map}

Tech stack: {tech_stack}

RULES:
1. Implement ONLY the endpoints defined in api_contracts above.
2. Request and response shapes must EXACTLY match the api_contracts schemas.
3. Every Python file must include all necessary imports at the top.
4. SQLAlchemy models must match database_schema exactly.
5. Do NOT invent any endpoint that is not in api_contracts.
6. Use async def for all route handlers.
7. Include proper error handling with HTTPException.
8. Use Pydantic models for request/response validation.
9. main.py must include CORS middleware allowing all origins.
10. Include a health check endpoint at GET /api/health.

OUTPUT SCHEMA:
{
  "files": {
    "backend/main.py": "# full file content here",
    "backend/models.py": "# full file content here",
    "backend/routes/example.py": "# full file content here"
  }
}

Respond with ONLY valid JSON matching the schema above. No markdown. No explanation.
No code fences. No text before or after the JSON object.
